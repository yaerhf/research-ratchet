<!-- DIET-CLASS: GOVERNING -->
# RESUME — written before a planned compaction, 2026-09-25

**Single use. Rename this file `RESUMED_2026-09-25.md` the moment you have read it** (C-38): a
resume brief read twice is a stale instruction presented as current, and `scripts/wakeup.py` reads
the newest `RESUME_*.md` it finds without being able to tell.

## What was happening when this was written

**Nothing was mid-flight.** The tree is clean, everything is banked and pushed through `3eaa46f`,
and the last act was adopting the founding tree's token measures (W26: nine as method, four adapted,
five refused). **There is no interrupted task to pick up** — which is why this compaction was
planned rather than forced, and the point of C-38's boundary rule.

## The next step, if the human does not name one

**W22 — the historian and the event spine — is the item whose design is settled and whose build has
not started.** The decisions, so they are not re-litigated: a **generated view (a)**, never a new
hand-kept spine · **one line per commitment change**, not per action (a rule added, changed or
retired; a tier moving; a dead end; a path opened, closed or promoted; a reversal; a founding slot;
a discharge) · **two tiers** — arc rollups, always read, plus a slice of the event spine keyed to the
ids and paths today's step touches · the whole spine only on a periodic sweep · the role's diet is
the spine and the ledgers, **starved of the current derivation**, and its question is *does today's
work contradict what this programme already learned?*

**Measured, so the sizing is not guessed again:** the founding tree carries 1308 commits and 1109
ledger rows; filtered to commitment changes, ≈415 rows ≈19k tokens. The optical tree: 129 commits,
≈440 rows ≈20k. **Ten times the history, the same spine.**

## What a summary would drop

- **The human's standing filter on imports**, which decided three items today: *a practice born of
  another tool's constraint is not a finding about research*, and **adopt another tree's reasoning,
  never its numbers** — ask first which role the rule was written for. He is not a research
  coordinator here and neither am I: **this seat is apparatus development**, and cost rules written
  for a coordinator's context do not transfer (that is why the commit voice stayed dense).
- **Three tools this week shipped a defect that only running them found** — the transcript reader's
  zero-width window, the pack gate reading a stamp instead of content, the wake-up printer taking a
  prose mention for a heading. The self-tests were green each time. **Run the thing.**
- **Owed to the human, unanswered by choice rather than oversight:** which submit path works for his
  self-compaction script (queued `Ctrl+X Enter` versus `Enter, Enter`) — he will test it; the wide
  pass is to be tried on Opticalc *when his weekly credits renew*; `rr-sabotage` is to be run
  against a repository nobody here has audited, his brother's `rimi.convention` being the candidate,
  **where two holes are already known by hand so the tool's unaided result can be checked**.
- **The blind stands** (`HANDOFF.md` § MY STANDING FENCE): this seat has not read any benchmark
  target's mathematics and must not. Methodology is safe to receive; the target's mathematics,
  solution path and literature are refused, with the reason said out loud.
