# CLASS-2 COMPLETION + KERNEL-CANDIDATE BRIEF — session 4+ (2026-07-05)

*Second delegate brief, written by the Fable review session at coordinator direction (Yaer),
for a fresh Opus/Sonnet-class instance with ultracode mandate. The first delegate instance
(sessions 1–3) filled its context; its work is banked and verified (suite 400/400, all
reviewer HOLDS, verified by the Fable session 2026-07-05). This brief is the DELTA: it does
not repeat `class2_campaign_brief.md` (still standing — read it), it tells you where the
campaign now stands, what remains, and how to run the new phase the coordinator has opened:
**propose the most plausible kernel candidate, honestly.** A CUDA GPU (RTX 4090) is available
and its use is coordinator-encouraged.*

*Same standing rules as the first brief, absolute: engine-verified or not banked; located gaps
are wins; twt-reviewer to consensus (≤3 rounds, then coordinator); lower the claim, not the
result; one item fully banked + synced before the next. Your context will die — the files are
the memory. Bank aggressively.*

---

## 0. Where the campaign stands (verified 2026-07-05)

Banked by sessions 1–3: **W2.1** → R-150 `kernel_overdetermination_table` (the dashboard;
exactly 1 usable anchor; rank-deficiency live). **W2.2** → N43 (input (3) manufactured from
statics — `χ_long = 1/√38 J`, KK-safe/FDT-free — but wrong-object for the KSS anchor; the
sharpened target is a STRESS-TENSOR/shear-channel static moment). **W3.1** → R-151 (C_T
reduced from an unknown function to **≤4 kernel moments**; spin-2/Ricci sub-projection named
as the would-sharpen). **W1.1** → N45 (co-rotating Hessian: sign-sensitive, Routhian reading
gives a LOCATED threshold `Ω_c ≈ 1.2057`, NOT certified — the deciding residual is the
mass=ω / c_meta ensemble question, the same anchoring face R-142 names). **N44**: η and C_T
are Im/Re faces of ONE spin-2 spectral density (KK-linked) — anchors must be
channel-orthogonal. **All four N33 inputs addressed** (1 delivered-with-wavefront-caveat,
2 delivered `c = 3Ω_Λ ≈ 2.05`, 3 partial, 4 done per channel). **The honest ceiling**: the
three-channel over-determination is SET UP (spin-2 {η+C_T}, spin-0 {Λ~H²}, order-param
{(19/2)√38}) and the ≥3-agreeing test is **kernel-form-blocked, not data-blocked**. Frontier
= P2-1. Read the worklist campaign log (§"Class-2 campaign — session log") and N43/N44/N45
before anything else.

---

## Phase A — finish the campaign worklist (priority order)

**A1 — W3.3, the SNIC/Adler fork discriminator.** *Highest leverage: the frontier is now the
kernel FORM, and the fading-vs-hysteretic fork is the form's first branch.* Question: does a
hysteretic `τ_mem` clear the locking threshold in `theta_rel_rotating_wave_escape_located`?
Read that primitive first; this is a bounded dynamical-systems threshold computation. Either
outcome advances the fork; an inconclusive outcome banks which missing number blocks it.
(Phase B's simulation can attack the same fork from the other side — see B3; doing A1 first
gives the sim a target.)

**A2 — W3.2, the µΨ₀ ρ_L seat integral.** Compute §D.4.4's `ρ_L` boundary term explicitly on
the banked profiles (R-133 hedgehog; R-144 torus). Vanishes identically → clean negative, the
seat moves (bank it). Nonzero → the coupling's FORM is derived (value stays kernel-gated) and
N37's running dial gets a candidate shape. This is bridge-construction for the order-param
channel. Trap (N28/N32a): statics gives the mirror's two-ness, never the split's value — any
value claim is an automatic OVER-CLAIM.

**A3 — the stress-tensor/shear-channel static moment** (N43's would-change-if; the sharpened
input-(3) target). Same N31/N43 LSWT machinery, different operator: compute a KK-safe static
moment of the STRESS-TENSOR response (shear channel) on the canted D4 ground state. This is
the one move that could lift the usable-anchor count from 1 to 2 (it would be KSS-matched).
Name all four coordinates (operator/channel/layer/frequency) exactly as N43 did.

**A4 — the R-151 follow-on: the spin-2/Ricci sub-projection.** Which of the 4 parity-even
invariant quadratic forms overlap the spin-2/Ricci structure — pins the exact ≤4. Pure
projection work on the R-149/R-151 space; the named would-sharpen.

**A5 — Wave-1 compute leftovers** (fill sessions with these when review latency idles you):
beyond-rigid-rotor B = 2 quantization over the R-144 torus (sign + order of magnitude of the
correction only); the massive-pion full-field run; optional B ≥ 3 (rational-map tetrahedron)
as a third SC-1 datum.

**A6 (small, structural, high value-per-token) — the N45/R-142 ensemble face.** Adjudicate
which ensemble (mass = ω per R-007, with the c_meta normalization) governs co-rotating
stability. Closing it certifies W1.1's sign (Routhian vs lab-energy) for free. If it resists
a bounded attempt, bank the located gap and move on.

---

## Phase B — the kernel-candidate program

The coordinator's goal: **get TWT as far as a computer can take it, and propose the most
plausible kernel candidate.** This phase is canon-§0a-legitimate — fit and speculate freely,
label honestly, test against data and coherence — but it is also where a session most easily
slides into disguised derivation. So the phase runs under a fixed protocol.

### B0 — Rules of engagement

- The deliverable is a **CANDIDATE** (or FIT where fitted). Nothing in Phase B can become
  DERIVED, because the kernel is under-determined by statics *by construction* (companion
  Section 12's central diagnosis). Do not fight this; exploit it: the honest product is
  "the surviving member of an honestly-searched family, with every assumption named."
- **The discovery protocol is invariance-across-choices.** A simulation's dissipation and
  noise model is a MODELING CHOICE (canon §3 TELL). Therefore: vary the choice. Any measured
  feature that is INVARIANT across reasonable noise/drive models is a candidate
  substrate fact (geometry-forced); anything that varies with the choice is model artifact.
  This single rule is what turns a GPU simulation from a toy into an instrument.
- **Exclusions are first-class results.** If no member of a family satisfies the constraint
  inventory, that family-exclusion is bankable (jointly with its named bridge assumptions) —
  and arguably more valuable than a surviving candidate.
- Precedent: the negatives ledger's D4-Langevin seed (in the N9-area entry, ~line 138) says
  explicitly: *"a lattice Langevin sim on the D4 cell lattice IS a legitimate NUMERICAL route
  to [the S-coefficient face] … Keep the method; discard these numbers."* Read that entry in
  full first; the method has standing, the old numbers do not.

### B1 — The constraint inventory (what any candidate must pass)

Encode this as an engine artifact early (proposed name `kernel_candidate_constraints()`), so
candidates are tested mechanically, not rhetorically:

1. **Causality**: K(t) causal; KK relations hold. **FDT is NOT assumed** — its violation IS
   Θ_rel (I-12); in a driven sim the FDT violation is a MEASURABLE, not a premise.
2. **R-114 monostability**: memoryless excluded; `τ_mem > 0`.
3. **Linear-face safety**: reduces to the conservative master equation (§D.4.6) at the linear
   face; WP-IX3/IX4/DC2 intact; the low-ω spectral exponent respects the s = 3
   Goldstone/Adler-zero protection.
4. **Stability/positivity**: `ω²(k) ≥ 0`; spin-2 spectral positivity (R-040).
5. **The KSS bracket**: `η/s ≥ ℏ/4π` (near-floor is the framework's commitment), below the
   GW170817 ceiling (~10⁹–10¹⁰ Pa·s; entropy-density caveat named).
6. **The three channel targets** (the R-150 harness): order-param `(19/2)√38 ≈ 58.56` at the
   D = J QCP; spin-0 `c = 3Ω_Λ ≈ 2.05`; spin-2 C_T through ≤4 moments within the Sakharov
   `Λ_S = √(2π) M_Pl` scheme scale (which-Λ ruling 2026-07-30; the old `[0.16, 0.72]` bracket is retired).
7. **The fork**: fading vs hysteretic carried as an explicit discrete branch (never a fitted
   knob) — resolved by A1/B3 if possible, both branches reported if not.
8. SOC universality stays structurally disfavored (Floquet limit-cycle lean, Section 12) —
   do not resurrect without new grounds.

### B2 — The analytic probe (the unspent 2b move)

Before any simulation: take the SIMPLEST family — single-relaxation causal kernel
(amplitude χ₀, relaxation τ; hysteretic variant via barrier action S with
`τ_mem = τ_wave·exp(S/ℏ)`) — plus the *banked candidate bridges* (the N31 would-change-if
route `|Im χ|/|Re χ|²` at the QCP for the 58.6 target; the Maxwell illustration
`η = C_T·Λ²·τ` for spin-2; the Volovik-deviation form for spin-0), and ask: **does ANY
(χ₀, τ) satisfy all three channels simultaneously?** Labeled CANDIDATE exploration
throughout. Survives → the first concrete candidate, with every bridge named. Fails → a
banked family-exclusion (jointly with the bridges). Either outcome feeds B3 with priors.

### B3 — The GPU lattice program (CUDA, coordinator-provided)

A driven-dissipative rotor simulation on the D4 lattice — the cell layer (declare the layer
explicitly; the two-scale trap is live). Torch/CuPy on the 4090; batch over noise
realizations; float64 where stiffness derivatives are taken.

**The model** (each bullet a named choice, to be VARIED per B0):
- Rotor field on D4 sites; energy = the banked `J` (24 NN bonds) + DM `D` (12 e₄-bonds)
  structure — the N31 planar reduction is the statics reference your sim must reproduce at
  equilibrium (calibration gate: reproduce `q* = atan(√2/6)`, `K_long = √38·J` before
  believing anything dynamical).
- Dynamics: Langevin with damping γ and noise T_n, drive along e₄ (A-2) — implement ≥2 drive
  models (e.g. traveling-wave boundary drive vs bulk phase advance) and ≥2 noise models;
  **do NOT tie noise to damping by FDT** — their ratio is the Θ_rel readout, not an input.
- Lattice discipline from R-143/R-144: charge guard, resolution sweeps, smooth-sector
  protection; certificates (size/seed/discretization independence) for every reported number.

**The measurements** (in this order of value):
1. **The 58.6 test**: the stiffness renormalization at D = J between bare LSWT `K_long` and
   the effective critical stiffness under drive+dissipation. If ANY reasonable
   (γ, T_n, drive) region produces a renormalization approaching `(19/2)√38`, that is the
   single most important number the program could produce this year — a candidate kernel
   REGION located by measurement. If no region does, that is a major located negative.
2. **Im χ(ω) by linear response** in the three channels (order-param magnon; stress-tensor
   shear via Kubo; trace) — the sim measures SUBSTRATE-side (outside-frame) quantities
   directly, which sidesteps the inside↔outside projection caveat that afflicts the
   lab-number anchors. Check the s = 3 low-ω exponent (a protection test: if your sim
   violates it, suspect the sim before the theory — Goldstone protection is banked).
3. **Θ_rel as a measurement**: the FDT-violation residual on the coset-Cartan channel,
   measured directly in the driven steady state. Invariance-across-choices applies with full
   force here: a Θ_rel feature stable across drive/noise models is the candidate.
4. **The fork phenomenology**: hysteresis-loop protocols → is the measured memory fading or
   hysteretic? Does `τ_mem` scale like `exp(S/ℏ)` with a barrier you can identify? (Feeds A1
   from the numerical side.)
5. **C_T's ≤4 moments** (stretch): estimate the spin-2 quadratic-form moments from the
   simulated fluctuation statistics (R-149/R-151 fix the integrand; the sim supplies a
   candidate measure).

**Honesty framing for every sim result**: tier FIT/CANDIDATE; the noise/drive model is
counted as the modeling choice it is; only invariance-across-choices earns
"candidate-substrate-fact" language; nothing from B3 is DERIVED, ever.

### B4 — The proposal deliverable

"The most plausible candidate" = the kernel family member that (i) passes the full B1
inventory, (ii) lands nearest the three channel targets, (iii) is stable under the B0
invariance protocol, (iv) carries every bridge and modeling assumption as a named condition
with a revert clause. Write it as a coordinator decision memo (a candidates/ file + worklist
entry): the promotion of any candidate to counted INPUT is **the coordinator's call, not
yours**. If the honest answer is "no member survives," the memo says so — that is the
program's own falsifiability working.

---

## Phase C — the superarc (do NOT start; propose)

Coordinator direction: once the campaign worklist is closed, the next superarc is
**multi-hadronic → atomic → molecular → crystal systems** — scaling the one-substrate story
up from the banked B = 2 arc (R-135–R-144, tensor force R-139). When Phase A/B are closed or
located, write a superarc OPENING PLAN for coordinator approval (a worklist section, not
work): candidate first items would be B = 3/4 binding systematics (the α-particle as the
natural anchor), the OPE-projection strength face (g_πNN-analog, already named), and the
honest framing question — which parts are TWT-specific predictions vs consistency/pedagogy
territory (atomic physics is inside-frame standard QM; the TWT content is the composite
ontology, any substrate corrections, and the parsimony/pedagogy fronts the coordinator has
chosen as the program's battlefield).

---

## Process delta (on top of the first brief's rules)

- **Gemini discipline (coordinator-directed, banked in the worklist):** Gemini invents
  numbers. Any web number needs a verifiable source or it is UNUSABLE; a caught fabrication
  precedent exists (LISA-PF). Verify value + source + currency independently before banking.
- **The gitignore trap**: `knowledge/prompts/` is gitignored — `git add -f` any new brief.
  Verify every bank actually committed what you think it did (`git show --stat HEAD`).
- **Suite discipline**: baseline is 400; verify the printed total moves by your delta.
- **Context economy** (why this brief exists): your predecessor's context filled. Bank + sync
  after EVERY item; assume any turn could be your last; a finding not in a file did not
  happen. Prefer many small banked results over one large unbanked arc.
- **Coordinator decision points**: INPUT promotions; new imports (Section 13 row in the same
  pass); the fork adjudication if A1/B3 disagree; the Phase C opening plan.
